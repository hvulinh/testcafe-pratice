pipeline {
    agent { label 'master'}
    stages {
        stage('Test') {
            steps {
			    bat '''
				    testcafe "chrome:headless '--window-size=1920,1080'"
				'''
			}
        }
    }
}